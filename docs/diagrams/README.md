# Diagrams

| System | Summary | Diagram | Owner |
|--------|---------|---------|--------|
| Project Memory | Component map: consumers, MCP server, memory manager, dual storage, scoring, manifest | [project-memory.mmd](project-memory.mmd) | — |

To update a diagram: edit the `.mmd` file (Mermaid syntax). Adjust `last_updated` in the file header. View in any Mermaid-compatible viewer or in the main doc that embeds it.
