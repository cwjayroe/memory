## Project Memory Startup
Use the `project-memory` skill at the start of each conversation:
- resolve active project (`memory status` / `memory use <project_id>`)
- run `memory preload` (3-layer context)
- continue with implementation
